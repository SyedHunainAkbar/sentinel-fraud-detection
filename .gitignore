# Data — never commit the real dataset (only data/sample/ and data/policies/ are allowed)
data/*
!data/sample/
!data/policies/
!data/README.md

# Trained artifacts
models/
reports/*.json
reports/*.png
!reports/demo/

# App assets for Streamlit Cloud (lightweight model files committed)
# app_assets/ is NOT ignored — it contains only small .joblib files + template CSV

# Secrets / Kaggle creds
.env
kaggle.json
*.key

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
