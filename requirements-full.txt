# Full development/CI dependencies
pandas>=2.0
polars>=0.20
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
xgboost>=2.0
imbalanced-learn>=0.12
shap>=0.44
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.33
pydantic>=2.0
joblib>=1.3
pytest>=8.0
pytest-cov>=5.0
httpx>=0.24
ruff>=0.4

# Optional: production features (copilot embedding retriever + LangGraph orchestration)
# Install separately: pip install boto3 langgraph
# The pipeline runs without these — TF-IDF retriever and plain-Python fallback are used.
boto3>=1.34
langgraph>=0.2
